name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build web app (capacitor mode)
        run: npx vite build --mode capacitor

      - name: Install Capacitor Android platform
        run: npx cap add android || true

      - name: Prepare app icon
        run: |
          mkdir -p assets
          cp "assets/Logopuslik.png" assets/icon.png
          cp "assets/Logopuslik.png" assets/icon-only.png
          cp "assets/Logopuslik.png" assets/icon-foreground.png
          cp "assets/Logopuslik.png" assets/icon-background.png

      - name: Generate icons & splash screen
        run: npx @capacitor/assets generate --android --assetPath assets

      - name: Sync Capacitor
        run: npx cap sync android

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Grant execute permission for gradlew
        run: chmod +x android/gradlew

      - name: Build APK
        run: cd android && ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
